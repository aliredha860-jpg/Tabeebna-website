module.exports = {
  // Update this once tabeebna.bh is connected (change it to "https://tabeebna.bh").
  // This should be the DOMAIN ROOT ONLY — no repo path here. The repo path
  // (e.g. /tabeebna-website/) is added automatically via pathPrefix + the
  // built-in `url` filter, so including it here would duplicate it.
  url: "https://aiiredha.github.io",
};
